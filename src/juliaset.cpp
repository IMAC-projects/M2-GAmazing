#include <iostream>
#include <stdlib.h>
#include <string>
#include <vector>
#include <algorithm>

/*
std::vector<c3ga::Mvec<double>> g_position;
c3ga::Mvec<float> g_zoom = 0.007f;
int g_maxIter = 30;
std::vector<c3ga::Mvec<double>> g_c(vector_e1_e2, -0.835f, -0.2321f);

// mouse position on last call to MouseButton() / MouseMotion()
std::vector<c3ga::Mvec<double>> g_prevMousePos;


void computeFractal(const std::vector<c3ga::Mvec<double>> &translation, const std::vector<c3ga::Mvec<double>> &c, c3ga::Mvec<float> zoom, int maxIter,
                    std::vector<unsigned char> &rgbBuffer, int width, int height) 
{
    int idx = 0;
}*/

int main()
{
    return 0;
}